using UnityEngine;

public class Collectables : MonoBehaviour
{
  private Coin[] collectables;
  // Start is called before the first frame update
  void Awake()
  {

  }

  // Update is called once per frame
  void Update()
  {

  }
}
